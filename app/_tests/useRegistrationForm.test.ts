import { useRegistrationForm } from "@app/register/hooks/useRegistrationForm";
import { renderHook, act } from '@testing-library/react';

describe('useRegistrationForm test', () => {
  it('', ()=> {

  })
})